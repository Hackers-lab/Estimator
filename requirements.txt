# Upper bounds prevent CI from silently pulling a breaking major release that
# only surfaces in the packaged (windowed) build.
PyQt6>=6.6,<6.11
openpyxl>=3.1,<4
pyinstaller>=6.0,<7
simpleeval>=1.0,<2
